@model IEnumerable<Artwork_App.Level>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Levels)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Levels)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LevelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LevelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LevelID })
        </td>
    </tr>
}

</table>
