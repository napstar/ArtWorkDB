@model IEnumerable<Artwork_App.ArtType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ArtType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ArtType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArtTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArtTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArtTypeID })
        </td>
    </tr>
}

</table>
