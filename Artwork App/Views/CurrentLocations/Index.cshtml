@model IEnumerable<Artwork_App.CurrentLocation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurrentLocation1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentLocation1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CurrentLocationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CurrentLocationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CurrentLocationID })
        </td>
    </tr>
}

</table>
